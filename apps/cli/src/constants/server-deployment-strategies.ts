import { withNone } from "@/utils/common";

export const SERVER_DEPLOYMENT_STRATEGIES_WITHOUT_NONE_MAP = {
	WRANGLER: "wrangler",
	ALCHEMY: "alchemy",
} as const;

export const SERVER_DEPLOYMENT_STRATEGIES_MAP = withNone(
	SERVER_DEPLOYMENT_STRATEGIES_WITHOUT_NONE_MAP,
);

export const SERVER_DEPLOYMENT_STRATEGIES_WITHOUT_NONE = Object.values(
	SERVER_DEPLOYMENT_STRATEGIES_WITHOUT_NONE_MAP,
);
export const SERVER_DEPLOYMENT_STRATEGIES = Object.values(
	SERVER_DEPLOYMENT_STRATEGIES_MAP,
);
